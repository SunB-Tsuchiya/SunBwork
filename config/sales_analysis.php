<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 取込プレビューキャッシュストア
    |--------------------------------------------------------------------------
    | 検証済みプレビューデータ（受注No・明細を含む暗号化JSON）は行数の多い部署だと
    | 数百KB規模になり得るため、既定のDBキャッシュ（cacheテーブルのvalue列は
    | TEXT=65,535byte上限）ではなくサイズ制約の緩いfileストアを使う。
    |
    | テスト実行時はphpunit.xmlでarrayストアへ切り替える。file固定のままだと、
    | テストをrootユーザーで実行した際にstorage/framework/cache/data配下へ
    | root所有のディレクトリが作られ、実サーバー（sailユーザー）が書き込めなく
    | なる事故が起きたため（2026-09-03実機検証で発覚）。
    */
    'import_preview_cache_store' => env('SALES_ANALYSIS_PREVIEW_CACHE_STORE', 'file'),
];
